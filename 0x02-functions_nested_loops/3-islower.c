#include "main.h"

/**
* _islower - function that check if passed character is lower or not
* @c: The character to be tested
*
* Return: 1 if @c is lower, otherwise it returns 0
 */
int _islower(int c)
{
	return ((c >= 97) ? 1 : 0);
}

#include "main.h"


/**
* _abs - computes absolute value of an integer
* @n: integer to calaculate its absolute value
*
* Return: The Abcoulute value of passed integer
 */
int _abs(int n)
{
	return ((n >= 0) ? n : -(n));
}
